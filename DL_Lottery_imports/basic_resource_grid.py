
class ResourceGrid:
    total_resource_blocks: int

    def __init__(
            self,
            total_resource_blocks: int,
    ) -> None:
        self.total_resource_blocks = total_resource_blocks
